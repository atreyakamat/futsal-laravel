## 1. Selected slots survive the login redirect

- [x] 1.1 In `components/BookingSystem.tsx`'s `fetchWindow`, change the `setSelectedSlots` reconciliation (~L131-142) to also add back any `activeSlots` entry with `status === 'selected'` not already present in `prev` (in addition to the existing filter that drops stale entries); verify by selecting a slot as a guest, logging in, and confirming the arena page shows it still selected with the correct total, with no change needed to the `/login?next=...` redirect itself.
- [x] 1.2 Verify the 30s poll and a manual page refresh don't fight an in-flight click: select a slot, immediately trigger `fetchWindow` (e.g. via the retry path), and confirm the selection isn't dropped or duplicated.

## 2. Lock the OTP-verified contact field on the profile page

- [x] 2.1 Thread `readAuthChannel()` into the response of `GET /api/dashboard/profile` (or an equivalent server-side read) so `app/dashboard/profile/page.tsx` can determine `lockedField: 'email' | 'mobile' | null`; verify the API response includes the channel for a logged-in customer and `null` for a pre-existing session with no `fg_auth_channel` cookie.
- [x] 2.2 In `app/dashboard/profile/page.tsx`, render the matching input `readOnly` with the same "(verified)" badge/lock icon treatment used in `components/CheckoutForm.tsx` (~L102-143); verify by logging in via each OTP channel and confirming the correct field is locked and the other stays editable.
- [x] 2.3 In `app/api/dashboard/profile/route.ts`'s `PUT` handler (~L100-167), add a server-side guard: read `readAuthChannel()` and reject the request with 400 if the payload changes the value of the channel-matching field; verify a direct API call attempting to change the locked field is rejected, and that changing the other field or non-contact fields (name) still succeeds.

## 3. Role-based admin header, arena_admin nav, and staff back-to-bookings on the QR page

- [x] 3.1 In `components/Header.tsx`, replace the path-based `isAdminPath` check (~L26) with a check against the known staff roles (`super_admin`, `arena_admin`, `manager`, `security`, `accountant`) so the admin header renders for staff on any page; verify a manager visiting a non-`/fg-admin` page (e.g. the booking success page) now sees the admin header.
- [x] 3.2 Add the missing `arena_admin` branch to the admin nav array (~L64-144), matching the existing per-role branch pattern (links to `arena_admin`'s available sections); verify an arena_admin no longer sees an empty nav bar.
- [x] 3.3 Add a minimal mobile nav disclosure to the admin header (it currently has none, unlike the customer header's drawer at ~L200-252), reusing the existing drawer pattern; verify the admin nav is reachable on a narrow viewport.
- [x] 3.4 In `app/booking/success/[ref]/page.tsx`, read `readAuthRole()` and change the bottom action row (~L283-296): for a staff role, replace "BOOK AGAIN" with a "BACK TO BOOKINGS" link to that role's own bookings list (`/fg-admin/arena/bookings` for manager, `/fg-admin/platform/bookings` for super_admin/arena_admin) and drop "MY BOOKINGS" (customer dashboard link); leave the customer-facing two-button row unchanged for customers. Verify by opening a booking's QR page as a manager and as a customer.

## 4. Manager slot blocking: permission fix + date range + beyond-window

- [x] 4.1 In `app/api/fg-admin/platform/slots/route.ts`, add `block_add`/`block_delete` to `MANAGER_ALLOWED_ACTIONS` (~L120) so manager submissions route through the existing approval-request path instead of 403ing; verify a manager's block submission now creates a pending approval request instead of failing.
- [x] 4.2 Rewrite the `block_add` handler in `app/api/fg-admin/platform/slots/route.ts` to accept `{ booking_date, end_date?, from_hour, to_hour, reason }`, expand server-side into a date list (`booking_date`..`end_date`, inclusive, defaulting `end_date` to `booking_date`) and an hourly-slot list (`from_hour`..`to_hour`); for `super_admin`/`arena_admin` insert one `admin_slot_blocks` row per (date × slot) directly, for `manager` create a single `createApprovalRequest` with `payload: { dates: string[], slots: string[], reason }`; verify a single-day/single-hour submission behaves exactly as today, and a multi-day/multi-hour submission produces the full cross product in one request.
- [x] 4.3 (merged into 4.2 — server-side expansion replaces the client-side per-call loop, so one submission is already one API call / one approval record; no separate task needed.)
- [x] 4.4 In `components/SlotManagementClient.tsx`, replace `handleAddBlock`'s per-hour `postOnce` loop (~L202-235) and the block form (~L394-413) with a Start Date/End Date + From/To hour form that sends one request matching the new `block_add` contract from 4.2; verify submitting a 3-day range with a 2-hour range creates the expected rows (direct, since this page is super_admin/arena_admin only in practice) in one action.
- [x] 4.5 Create `components/ManagerBlockClient.tsx` (new file, mirroring `components/ManagerHolidayClient.tsx`'s structure: own fetch of `GET /api/fg-admin/platform/slots`, same POST pattern) with a Start Date/End Date + From/To hour + Reason form posting `block_add`, plus a list of the arena's current blocks with an "Unblock" action (`block_delete`, direct for manager per the route's existing scoped-delete branch); wire it into `app/fg-admin/arena/slots/ArenaSlotClient.tsx` alongside `ManagerHolidayClient`. Verify a manager can submit and see a block request end-to-end from `/fg-admin/arena/slots`.
- [x] 4.6 Add a `BLOCK_SLOT_REQUEST` case to `ArenaSlotClient.tsx`'s `RequestTypeBadge`/`RequestDetail` (~L44-69) so a manager's own block requests show up correctly in that page's "Operational Requests" history (they already appear via the existing `approvalRequests` query — only the label/detail rendering is missing); verify a submitted block request shows dates/slots/status in that list.
- [x] 4.7 Update `PayloadSummary` in `app/fg-admin/platform/approvals/page.tsx` (~L159-173) to read `payload.dates` (array) instead of `payload.bookingDate` (single); verify a multi-day request displays all its dates in the approvals UI.
- [x] 4.8 Update `summarizeApprovalPayload` in `lib/admin.ts` (~L481-499) to summarize `payload.dates` in the approval-request notification email; verify the email/notification text lists all dates instead of only a single one.
- [x] 4.9 Update the approval-decision handler in `lib/admin.ts` (~L952-975) to read `payload.dates` and loop `dates × slots` when inserting `admin_slot_blocks` rows on approval; verify approving a multi-day request creates one row per (date, slot) pair.
- [x] 4.11 (found after initial implementation, via user screenshot) `app/fg-admin/platform/super-admin/SuperAdminDashboardClient.tsx`'s own inline "Approvals" tab has a second, separate approval-request renderer (distinct from `app/fg-admin/platform/approvals/page.tsx`'s `PayloadSummary`) that always dumped raw `payload_json` — unreadable once a `BLOCK_SLOT_REQUEST` payload holds an 18-date array. Added `formatBlockSlotPayload`/`ApprovalPayloadDetail` there to render a compact Date(s)/Slots summary for `BLOCK_SLOT_REQUEST`, falling back to the existing raw-JSON box for every other request type (and for a `BLOCK_SLOT_REQUEST` whose payload doesn't parse); verified via `tsc`.
- [x] 4.10 Verify end-to-end that a manager can block a date beyond the current 15-day booking window (no client or server date-max exists today — confirm this is still true after the above changes) and that, once approved, the block correctly applies once that date enters the booking window (cross-check against `/api/slots/status`'s existing window/holiday/block precedence).

## 5. Nav de-duplication

- [x] 5.1 In `components/ProfileMenu.tsx`, for each staff role branch (`super_admin` ~L94-129, `manager` ~L131-166, `arena_admin` ~L168-195, `security` ~L197-216), remove links whose destination now duplicates an entry in that role's Header nav (per the role-based header from task 3.1-3.2); keep Logout and "Signed in as" for every role, and leave the `customer`/`player` branch (~L73-92) unchanged; verify each staff role's dropdown no longer repeats a Header nav destination.
- [x] 5.2 In `app/fg-admin/platform/super-admin/SuperAdminDashboardClient.tsx`'s sidebar (~L672-743), remove direct links that exactly duplicate a Header nav destination for super_admin (e.g. All Bookings, Cancellations Review, Reviews Moderation, Slot Pricing, Reports, GST Documents, Accountants, Audit Logs, Notifications), keeping the in-page tab panels (Overview, Arena & Staff, Timings, Block Slots, Approvals, Settings) and stats intact; verify the sidebar no longer duplicates Header nav links but dashboard functionality is otherwise unchanged.
- [x] 5.3 In `app/fg-admin/arena/dashboard/page.tsx` and `app/fg-admin/platform/dashboard/page.tsx`, remove card links that exactly duplicate a Header nav destination for that role, keeping any card that surfaces dashboard-specific content (stats, non-nav actions); verify the manager and arena_admin dashboards still function with the trimmed card set.

## 6. Verification pass

- [x] 6.1 Run `npx tsc --noEmit -p .` and confirm no new type errors across all changes. (One pre-existing error in `tests/unit/superadmin-identity-regression.test.ts:459`, unrelated to any file touched by this change.)
- [x] 6.2 Run `npx vitest run --pool=forks` and confirm no new failing tests beyond any already-known, pre-existing failures. (9 failures, all in `tests/assagao.test.ts` and `tests/unit/email-profile-update.test.ts` — the same two pre-existing, seed-data-dependent failures documented in `openspec/changes/fix-booking-checkout-and-rbac-issues/tasks.md` 7.2. 209 passed, no new failures.)
- [x] 6.3 Manually walk each scenario end-to-end in the dev server (via the `run` skill). Done: started the dev server cleanly (no compile/runtime errors), confirmed `/`, `/login`, `/dashboard/profile`, `/fg-admin/login`, and `/arena/aiem-assagao` all return 200 with no server-side errors, and `/fg-admin/arena/slots` / `/fg-admin/arena/dashboard` correctly 307-redirect when unauthenticated. Attempted to log in as the seeded super_admin (`superadmin@example.com` / default password from `scripts/db-init.cjs`) to drive an authenticated walkthrough of the Header/nav/slot-blocking changes, but the credentials were rejected (401) — this database was not seeded via that script, and no other known-working staff credentials were available. Did not run `db:init`/`db:setup` against the connected database to manufacture credentials, since that could reset or alter real data without explicit approval. **Not done**: authenticated click-through as manager/arena_admin/super_admin (role-based header rendering, ManagerBlockClient submit/approve/unblock flow, profile field lock behavior in the browser, QR page back-button). Recommend the user do a quick manual pass with real staff credentials before/while deploying, or share test credentials to continue this.
