## 1. Shared admin layout + sidebar

- [x] 1.1 Create `components/AdminSidebar.tsx` (client component: `usePathname`, `useSearchParams`, own `mobileOpen` state). Move the exact per-role `adminNavItems` arrays (`super_admin`, `arena_admin`, `manager`, `security`, `accountant`) out of `components/Header.tsx` into this file, rendered as a vertical desktop sidebar plus a mobile hamburger + slide-in drawer using the same item list; add a `Settings` entry for `super_admin` pointing at `/fg-admin/platform/super-admin?tab=settings` and an `Accountants` entry pointing at `/fg-admin/platform/accountants` (promoted from the dashboard's own sidebar). Verify: item hrefs/labels match what `Header.tsx` rendered before, one-for-one, plus these two additions.
- [x] 1.2 Create `app/fg-admin/layout.tsx` (server component): resolve `role`/`arenaId` the same way `app/layout.tsx` does; if role is a staff role render `<div className="flex">`+`<AdminSidebar/>`+`<main className="flex-1 min-w-0">{children}</main>`; otherwise render `{children}` unchanged (covers `/fg-admin/login` and any non-staff edge case). Verify: `/fg-admin/login` renders with no sidebar; an authenticated staff page renders with the sidebar present.
- [x] 1.3 In `components/Header.tsx`, remove the admin branch's `adminNavItems` arrays, the desktop `<nav>` block, the mobile hamburger button, and the mobile drawer — leave logo + `<ProfileMenu/>` only (the branch no longer needs `mobileMenuOpen` state itself; leave the customer-facing branch untouched). Verify: an admin page shows a slim top bar (logo + profile menu, no nav links) and the new sidebar carries all navigation.

## 2. De-duplicate the super admin dashboard's Approvals tab

- [x] 2.1 In `app/fg-admin/platform/super-admin/SuperAdminDashboardClient.tsx`, remove the `{ id: 'approvals', ... }` tab-button entry, the `activeTab === 'approvals'` render block, `fetchApprovals`, `handleResolveRequest`, the `approvals`/`approvalsLoading` state and their driving `useEffect`, and the now-unused `formatBlockSlotPayload`/`ApprovalPayloadDetail` helper functions above the component. Verify: `tsc` reports no unused-code type errors and no remaining reference to any of the removed identifiers.
- [x] 2.2 Confirm `AdminSidebar`'s "Approvals" entry (from 1.1) points at `/fg-admin/platform/approvals` and nowhere else links to the removed in-page tab. Verify by grep for `activeTab === 'approvals'` / `'approvals'` tab id returning nothing.

## 3. Horizontal tab bar + Settings deep link

- [x] 3.1 In `SuperAdminDashboardClient.tsx`, change the tab-button list's container from the current left `<aside className="w-64 ...">` to a horizontal bar (e.g. `flex gap-2 overflow-x-auto` inside the main content column, above the tab panels) — no changes to the tab buttons' click handlers or the `activeTab` state itself. Verify: the page renders with one left-hand menu (the new global `AdminSidebar`) and the former tab list now sits as a horizontal strip at the top of the page's own content.
- [x] 3.2 Replace `const [activeTab, setActiveTab] = useState('overview')` with a `useSearchParams()`-backed initial value (`searchParams.get('tab') || 'overview'`), keeping `setActiveTab` as local state for subsequent clicks (no need to push the tab into the URL on every click — only the initial load needs to honor the query param). Verify: navigating to `/fg-admin/platform/super-admin?tab=settings` lands directly on the Settings tab; navigating with no query param still defaults to Overview.

## 4. Relocate the super-admin email-change form; delete the dead settings page

- [x] 4.1 In `app/api/fg-admin/super-admin/settings/route.ts`'s `GET` handler, add `first_name`/`last_name` to the returned `data` object. Correction from design.md: `getSuperAdmin()` in `lib/super-admin.ts` didn't actually select those columns yet (its query only selected id/email/password_hash/is_active/last_login) — added them to that query and its return type too, one level deeper than assumed. Verify: hitting the endpoint while logged in as super_admin returns `first_name`/`last_name`.
- [x] 4.2 Add `first_name`/`last_name` to the `SuperAdminSettings` interface in `SuperAdminDashboardClient.tsx`, import `ChangeSuperAdminEmailForm` from `@/components/ChangeSuperAdminEmailForm`, and render it inside the `activeTab === 'settings'` block (e.g. above "Platform Configuration") with `currentName={`${settings.first_name ?? ''} ${settings.last_name ?? ''}`.trim()}` and `currentEmail={settings.email}`. Verify: the Settings tab shows a working "Super Admin Email" form that successfully updates the email via `PUT /api/dashboard/profile` (already-working endpoint, confirmed in a prior change).
- [x] 4.3 Delete `app/fg-admin/platform/settings/page.tsx`. Verify: `tsc` passes (no remaining import references it) and no route in the app links to `/fg-admin/platform/settings` (grep confirms only the two stale references handled in 4.4/4.5 remain, both fixed there).
- [x] 4.4 In `app/fg-admin/platform/dashboard/page.tsx`, remove the super_admin-gated "Settings" Quick Action `<Link href="/fg-admin/platform/settings">` card (dead code — this page redirects super_admin away before it could ever render). Verify: no reference to `/fg-admin/platform/settings` remains in this file.
- [x] 4.5 In `app/fg-admin/platform/audit-logs/page.tsx`, update the code comment citing `app/fg-admin/platform/settings/page.tsx` as an example of the auth-check pattern to instead cite `app/fg-admin/platform/credentials/page.tsx` (same pattern, still exists). Verify: no reference to `/fg-admin/platform/settings` remains in this file.

## 5. Verification pass

- [x] 5.1 Run `npx tsc --noEmit -p .` and confirm no new type errors beyond the one pre-existing, unrelated failure already known (`tests/unit/superadmin-identity-regression.test.ts:459`). (Also hit a stale `.next/types/validator.ts` reference to the just-deleted settings route — cleared `.next` to regenerate; not a real source error.)
- [x] 5.2 Run `npx vitest run --pool=forks` and confirm no new failing tests beyond the two already-known, pre-existing, seed-data-dependent failures (`tests/assagao.test.ts`, `tests/unit/email-profile-update.test.ts`). (209 passed, 9 failed — all in those same two files.)
- [x] 5.3 `grep -r "platform/settings"` across `app/` and `components/` and confirm zero remaining references (route fully removed from navigation and code). Only match: the explanatory comment left in `app/fg-admin/platform/dashboard/page.tsx` documenting why the tile was removed — not a live reference.
- [x] 5.4 Manually walk through the dev server (via the `run` skill) to the extent authentication allows. Done: started the dev server cleanly (no compile errors), confirmed `/` and `/fg-admin/login` render 200 with clean compile logs. Confirmed via `middleware.ts` that every `/fg-admin/platform/*`, `/fg-admin/arena/*`, `/fg-admin/security/*`, `/fg-admin/accountant/*` path is role-gated at the Edge middleware layer — an unauthenticated request 307-redirects to login *before* `app/fg-admin/layout.tsx`/`AdminSidebar` ever run, so (as in the prior change) they could not be exercised live without working staff credentials, which remained unavailable this session. **Not done**: authenticated visual confirmation of one sidebar + slim top bar per role, the horizontal tab strip, and the `?tab=settings` deep link actually landing on the Settings tab with the relocated email form visible. Verified instead via `tsc` (clean) and careful reading of the final diff. Recommend a manual pass with real staff credentials before/while deploying.
