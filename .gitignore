*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
.next/
/bootstrap/cache/*
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
# Turf photo uploads — stored outside .next so a rebuild/redeploy never wipes
# them (see lib/arena-image-upload.ts). Must exist as a real host directory,
# not regenerated content, so it's excluded from git rather than deleted.
/storage/uploads
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

/generated/prisma
db_data/
temp_pg_data/
github_actions_key
github_actions_key.pub
deploy_keys/*.pem
deploy_key.pem
temp_ssh_key
*.pem
ssh_keys.txt

# TypeScript incremental build cache — rewritten on every build, so keeping
# it tracked means every deploy leaves the working tree "dirty" and blocks
# the next git pull (exactly what caused repeated deploy conflicts).
tsconfig.tsbuildinfo

# scratch/debug output — never belongs in version control
err.txt
out.txt
test.txt
logs.txt
logs2.txt
consolidated_docs.txt
project_tree*.txt
backdate_commits.ps1
create_commits.ps1
old_sms.ts
console.error(e))
